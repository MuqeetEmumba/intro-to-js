let attendence = 33;

if (attendence < 34) {
    console.log("Let's fill this capacity");
    // else if allow us to make decisions that are linked but separate 
    // When you're doing if else, if else, you can have one if, multiple else ifs, and a single else, and you go from the top all the way down. 
    // And it'll try each condition until it either evaluates to true or finally gets to the else.
} else if (attendence > 35) {
    console.log("We're at over capacity");
} else {
    console.log("We're at full capacity");
}