const names = ["Aneeq", "Sarah", "Muqeet"]
// A for loop is for repeating a task a set number of times
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}


const numbers = [1, 2, 3]
for (let j = 0; j < numbers.length; j++) {
    console.log(numbers[j] * 2);
}

let number = [3, 6, 9]
for (let k = 0; k < number.length; k++) {
    console.log(number[k] * 3);
}

